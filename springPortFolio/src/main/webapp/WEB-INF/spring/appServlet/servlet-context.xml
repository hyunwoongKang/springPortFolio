<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	
	<annotation-driven />
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 업로드를 위한 bean 등록 -->
	
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="maxUploadSize" value="10485760"></beans:property>
	</beans:bean>
	
	<!-- 업로드 이미지 경로 등록 -->
	<beans:bean id="uploadPath" class="java.lang.String">
		<beans:constructor-arg
			value="/Users/woong/Desktop/upload">
		</beans:constructor-arg>
	</beans:bean>


	<!-- 인터셉터 -->
	<beans:bean id="loginInterceptor" class="org.test.interceptor.LoginInterceptor"></beans:bean>
	<interceptors>
		<interceptor>
			<mapping path="/user/login"/>
			<mapping path="/user/updateUserInfo"/>
			<mapping path="/uploadUserImage"/>
			<beans:ref bean="loginInterceptor"/>
		</interceptor>
	</interceptors>
	
	<!-- 메인 페이지 최근 본 게시판 쿠키 -->
	<beans:bean id="readBoardInterceptor" class="org.test.interceptor.ReadBoardInterceptor"></beans:bean>
		<interceptors>
			<interceptor>
				<mapping path="/board/read"/>
				<beans:ref bean="readBoardInterceptor"/>
			</interceptor>
		</interceptors>
		
	<!-- 관리자 페이지 접근 금지 -->
	<beans:bean id="adminPageInterceptor" class="org.test.interceptor.AdminInterceptor"></beans:bean>
		<interceptors>
			<interceptor>
				<mapping path="/admin/*"/>
				<beans:ref bean="adminPageInterceptor"/>
			</interceptor>
		</interceptors>
	
	<!--RSA처리를 위한 인터셉터 -->
  	<beans:bean id="rsaInterceptor" class="org.test.interceptor.RsaInterceptor"></beans:bean>
	   <interceptors>
	   		<interceptor>
	   			<mapping path="/"/>
	   			<mapping path="/user/joinUser"/>
	   			<mapping path="/board/list"/>
	   			<mapping path="/board/read"/>
	   			<beans:ref bean="rsaInterceptor"/>
	   		</interceptor>
	   </interceptors> 
	 
	<context:component-scan base-package="org.test.controller" />
	
</beans:beans>
